/**
 * Lógica de negócio pura (motor de fluxos, campanhas). Módulos daqui NÃO
 * dependem de HTTP — são chamáveis tanto pela camada web serverless quanto,
 * na V2, pelo worker Baileys. Vazio por ora; preenchido nas Fases 3 e 4.
 */
export {};
